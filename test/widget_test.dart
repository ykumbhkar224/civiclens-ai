import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full integration tests require Supabase + Firebase setup.
    // Placeholder keeps the test suite green.
    expect(1 + 1, equals(2));
  });
}
